wget https://github.com/zaproxy/zaproxy/releases/download/v2.16.1/ZAP_2.16.1_Linux.tar.gz
tar xvf ZAP_2.16.1_Linux.tar.gz
sudo ln -s /usr/local/share/zaproxy/zap.sh /usr/local/bin/zap

cd /usr/local/share/
sudo git clone --branch v2.5.0 --depth 1 https://github.com/sullo/nikto.git
sudo chmod +x nikto/program/nikto.pl
sudo ln -sf /usr/local/share/nikto/program/nikto.pl /usr/local/bin/nikto

sudo apt install pipx
pipx install "wapiti3==3.2.10"
pipx ensurepath # chyba trzeba restart sesji

sudo snap install go --classic --channel=1.25/stable
go install -v github.com/projectdiscovery/nuclei/v3/cmd/nuclei@v3.4.10
sudo mv /home/michal/go/bin/nuclei /usr/local/bin/
zaproxy -daemon -port 8080 -config api.disablekey=true


RUN apt-get update && apt-get install -y \
    chromium \
    chromium-driver \
    fonts-liberation \
    libnss3 \
    libasound2 \
    libatk1.0-0 \
    libc6 \
    libcairo2 \
    libcups2 \
    libdbus-1-3 \
    libexpat1 \
    libfontconfig1 \
    libgcc1 \
    libglib2.0-0 \
    libgtk-3-0 \
    libnspr4 \
    libpango-1.0-0 \
    libpangocairo-1.0-0 \
    libstdc++6 \
    libu2f-udev \
    libx11-6 \
    libx11-xcb1 \
    libxcb1 \
    libxcomposite1 \
    libxcursor1 \
    libxdamage1 \
    libxext6 \
    libxfixes3 \
    libxi6 \
    libxrandr2 \
    libxrender1 \
    libxss1 \
    libxtst6 \
    ca-certificates \
    wget \
    && rm -rf /var/lib/apt/lists/*

export NUCLEI_HEADLESS_CHROME_PATH=/usr/bin/chromium
